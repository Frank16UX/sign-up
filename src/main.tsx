import React from 'react'
import ReactDOM from 'react-dom/client'
import SignUpFlow from '../pampered-chef-signup'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SignUpFlow />
  </React.StrictMode>,
)
